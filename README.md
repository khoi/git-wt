# git-wt

Just some dead simple sugar on top of git worktree in Bash. 

## Installation

Just copy it to ur PATH

## Usage

```
git wt new   create a new worktree and cd into it
git wt list  list worktrees
git wt ls    alias for list
```
